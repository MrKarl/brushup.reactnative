/** @format */

import {AppRegistry} from 'react-native';
import SplashComponent from './SplashComponent';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => SplashComponent);
